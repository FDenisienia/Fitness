# Copia este archivo como .env.local en la carpeta backend (el nombre empieza con punto).
# Ahí va solo la URL de MySQL local; Railway sigue en .env

DATABASE_URL="mysql://root:devpassword@localhost:3306/athlento"
