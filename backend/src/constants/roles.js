/** Valores persistidos en `User.role` */
export const ROLES = Object.freeze({
  ADMIN: 'admin',
  COACH: 'coach',
  CLIENTE: 'cliente',
});

export const ROLE_LIST = Object.freeze([ROLES.ADMIN, ROLES.COACH, ROLES.CLIENTE]);
